---
import Layout from "../../layouts/Layout.astro";
import GuestBook from "../../components/views/GuestBook.astro";
import { t } from "../../i18n";

const lang = "en" as const;
const d = t(lang);
---

<Layout lang={lang} route="guestbook" title={d.nav.guestbook}>
  <GuestBook lang={lang} />
</Layout>
